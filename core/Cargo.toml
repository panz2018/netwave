[package]
name = "netwave"
version = "0.0.1"
edition = "2024"
rust-version = "1.98"
publish = false
description = "netwave core: network scattering-parameter math (scaffold phase 0)"

[dependencies]
num-complex = "0.4"

[dev-dependencies]
criterion = { version = "0.8", features = ["html_reports"] }
num-complex = "0.4"

[[bench]]
name = "scaffold"
harness = false
