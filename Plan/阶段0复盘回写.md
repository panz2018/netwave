# 阶段 0 复盘回写清单（待执行）

来自 `phase0-monorepo-scaffold` 实施过程（2026-09 阶段 0）。每条完成后勾选；
全部完成且结论已进各目标文档后**删除本文件**（见 [README.md](README.md) 生命周期）。

README 尚未建立前，先回写 [开发流程.md](开发流程.md) 技能地图；
README 拆分落地时（见 [README拆分规划.md](README拆分规划.md)）再迁至对应 README。

## typescript（细节已沉淀 `typescript/README.md`；人工审核仍未完成）

- [x] napi：`Buffer::from` 零拷贝内部 `mem::forget`；`from_raw_parts` 重建时
      capacity 必须传 `v.capacity()*16`，否则 double-free → 已进 README
- [x] napi：直接返回 `Vec` 会退化为逐元素 JS Array，必须显式 `Buffer` 类型
      → 已进 README
- [x] napi CLI 旗标：`--manifest-path`（非 `--cargo-cwd`）、`--format commonjs`
      （非 `cjs`）——版本漂移点，升级后必查 → 已进 README
- [x] wasm：`js_sys::Memory` 不存在，用 `js_sys::WebAssembly::Memory`
      → 已进 README
- [x] wasm：web target 选型原因（bundler glue 在 node/vitest 不可加载，
      web glue 两端通用）→ 已进 README（长期与否待拍板，见文末）
- [x] wasm：init 传 `{module_or_path: bytes}`（Node 无 `fetch file:`）；
      字符串/URL 形态已废弃 → 已进 README
- [x] wasm：沙箱下载不了 binaryen → 已解决（2026-09-24）：
      `scripts/install_binaryen.sh` 动态取 GitHub latest release 装进
      `~/.local/bin`（本地与 CI 共用同一脚本，不钉版本——钉死会腐烂，
      不兼容就让构建报错当场修）；wasm-pack 内置下载钉在老版 117
      （优化差、体积反增），必须 PATH 覆盖。`wasm-opt = false` 已删，
      产物 35KB→20KB（-43%）
- [x] worker：wasm 线性内存绝不能 transfer（detach 实例）；
      `.slice()` 复制进新 ArrayBuffer 再 transfer → 已进 README
- [x] 壳文件体系：`publish_shell.mjs` 的 `../dist/` → `./` 重写规则
      → 已进 README（含「只改 src/，不手改 dist/」）

## python（人工审核已通过 2026-09-25，细节已沉淀 `python/README.md`）

- [x] `crate-type = ["cdylib"]` 防 E0464（无哈希 rlib 与 core 哈希 rlib 撞名机制）
      → 已进 `python/README.md` Implementation notes
- [x] 改 core 后必须重新 `maturin develop`，否则对拍用旧产物
      → 已进 `python/README.md` Gotchas
- [x] `python_relative: 0.0` 逐 bit 容差的依据（numpy 视图直通，无重算）
      → 已进 `python/README.md` Implementation notes

## core / 测试

- [x] 对拍 dump 必须二进制 `.bin`（`JSON.stringify(-0)` → `"0"` 丢符号位）
      → 已进 `core/README.md` Gotchas + `testdata/README.md` 完整契约 +
      `scripts/README.md` Notes
- [x] `#[coverage(off)]` 是 nightly 专属 → 已进 `core/README.md` Gotchas；
      JS 侧 `/* v8 ignore start/stop */` 须附理由注释 → 已进 `typescript/README.md` Gotchas

## 根 / 环境

- [x] 沙箱环境三件套：`LD_LIBRARY_PATH`（uv python lib）+ `VIRTUAL_ENV` +
      `PYO3_PYTHON`（`/lsiopy` 是过期残留）→ 已由 `.vscode/settings.json` 根治
      （可移植变量引用）；根 README 建立时补一句指针
- [x] corepack 必须 `COREPACK_ENABLE_DOWNLOAD_PROMPT=0`（防交互假死）
      → 已进 `AGENTS.md` 终端命令规范
- [x] 终端 cwd 会漂移：跨目录命令一律绝对路径 / `--manifest-path` / `-C`
      → dump 脚本已改为打印绝对路径；`core/README.md` 注明 repo-root 运行
- [x] 改 core 后跑任何绑定测试前，先重建该绑定产物
      （maturin develop / napi build / wasm-pack build）
      → 已进 `core/README.md` + `python/README.md` Gotchas

## 依赖升级（2026-09-24 criterion 0.8 / napi 3.13 / pyo3+numpy 0.29）

- [x] 升级验证链（缺一环就会拿旧产物自欺）：`cargo update` →
      `cargo check/test --workspace` → maturin 重建 + pytest →
      napi 重建 + vitest → **四端对拍**。本次全链零代码改动通过，是基线样本。
      → 链已固化进 `AGENTS.md` 依赖版本策略（配对依赖必跑四端对拍）
- [x] "是否全最新"一行体检：`cargo update --dry-run`（含间接依赖，
      比逐个 `cargo search` 快）→ 已进 `AGENTS.md` 依赖版本策略
- [x] napi 3.13 在容器里的
      `Skipping cross-process filesystem reconciliation lock`
      是无害警告，可忽略，别当错误排查
      → 已进 `typescript/README.md` Gotchas

## CI 格子恢复（等各自人工审核）

- [x] core 已本地人工审核通过（2026-09-24）；CI 保留 core 四平台格
      （linux-x64/arm64、windows-x64、darwin-arm64）+ bench 格。
- [x] python 已人工审核通过（2026-09-25）：恢复 workspace member +
      CI python 双版本格（3.10/3.14，含 ruff）。
- [ ] 禁用中：wasm32 格与 node job（typescript 审核通过后取消注释，
      注释处有日期标记；同时从根 `Cargo.toml` exclude 移除
      `typescript/native`、`typescript/wasm` 并加回 members）

## 待拍板决策

- [ ] web target 是长期约束还是阶段 0 临时选择？长期 → 升格进
      [constitution.md](constitution.md)
- [ ] constitution 未来迁 `openspec/project.md`（见 [README.md](README.md) 生命周期）
