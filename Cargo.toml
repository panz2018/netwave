[workspace]
resolver = "2"
members = ["core", "python", "typescript/native", "typescript/wasm"]

# Release profile: LTO for wasm binary size (spec: zero-copy-roundtrip).
[profile.release]
lto = true
opt-level = 3
